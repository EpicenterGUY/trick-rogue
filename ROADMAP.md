# 트릭로그 프로토타입 개발 이정표

기준: 2026-08-26

이 문서는 프로토타입 단계의 우선순위를 고정한다. 카드 수를 빠르게 늘리는 것보다, 핵심 전투와 런이 재미있는지 빠르게 시험하고 수정할 수 있는 구조를 먼저 만든다.

현재 `main`은 M4 초입을 넘어 **RUN V3 8스테이지 구조 + 빌드 아이덴티티 개편까지 통합된 상태**다. 따라서 당분간 신규 시스템 확장보다 통합 안정화와 실제 플레이테스트를 우선한다.

## 원칙

- 게임 규칙상의 **카드팩 개념을 사용하지 않는다.**
- 지역은 카드군이나 클래스가 아니다. 보상 성향은 조정하지만 카드 소속을 제한하지 않는다.
- 신규 효과 카드는 한 번에 정해진 수를 억지로 채우지 않고 **2~5장 단위**로 추가한다.
- 새 카드는 기존 트릭·쇼다운·트럼프·칩·리버·손패·슬롯 판단을 더 재미있게 만드는지 먼저 검증한다.
- 프로토타입 동안은 새 자원/새 하위 시스템을 쉽게 늘리지 않는다.
- 자동 테스트와 실제 브라우저 체감 테스트를 둘 다 통과한 뒤 다음 대규모 확장으로 넘어간다.
- 코드상 계산 결과와 HUD·카드 설명·용어집 표시는 반드시 같은 규칙을 보여야 한다.
- 런 진행 중 카드·필드·특성·보상처럼 되돌리기 어려운 상태 변화는 묵시적으로 소실되지 않게 한다.

## 현재 main 스냅샷

2026-08-26 기준 최근 통합 내용:

- RUN V3: **8스테이지 런**, 지역 3종 중 2곳 방문, 지역 내부 분기, 최종 관문, 최종지역
- 이벤트 **10종**, 미니게임 **5종**
- 스타터 **4종**, 특성 **8종**
- 필드 **8종**
- 빌드 태그 **6계열**
- 빌드 아이덴티티용 신규 네임드 **12장**
- M4 특이 카드 실험은 1~4차 배치까지 진행되어 **총 12장**이 구현·자동 검증된 상태

이정표의 즉시 목표는 위 기능을 더 늘리는 것이 아니라 **통합 플레이에서 틀어지는 규칙·상태·UI를 잡는 것**이다.

## M1. 효과 카드 풀 평탄화 — 완료

- 순수 카드 52장은 독립 유지.
- 공용 효과 카드와 기존 추가 효과 카드를 팩 선택 없이 사용한다.
- 옛 `pack01.*`, `pack02.*` 식별자는 저장 호환을 위해 당분간 유지하되 게임 규칙상의 팩을 뜻하지 않는다.
- 신규 `pack03` 및 지역 연계 카드군 계획은 폐기한다.

완료 PR: #106

## M2. 개발자 테스트 모드 — 완료

일반 플레이 파일을 복제한 별도 개발자판 대신 같은 앱을 `?dev=1`로 실행한다.

1차 목표 — 완료 (#107):

- 모바일에서도 열 수 있는 고정 `DEV` 버튼과 접이식 패널
- 개발자 모드에서만 상태 요약 표시
- 플레이어 HP / 적 HP / 칩 / 보호막 직접 변경
- 트럼프 강제 지정
- 카드 검색 및 덱/손패 추가 진입점
- 일반전 / 엘리트 / 보스 빠른 시작 진입점
- 전투 상태 빠른 초기화
- 일반 URL에서는 개발자 UI와 조작 기능을 노출하지 않음
- 가능한 기존 `debug=1` 진단 기능을 포함하는 상위 모드로 동작

후속 확장 — 완료:

- [x] 쇼다운 슬롯 직접 배치 (#108)
- [x] 세트 승/패/무 및 순서 지정 (#109)
- [x] 상태·우세·리버·예약 효과 강제 적용 (#109)
- [x] 쇼다운 즉시 실행 (#108)
- [x] RNG 시드 고정 (#110)
- [x] 효과 발동/최종 위력 계산 로그 (#108, #109)
- [x] 개발자 세이브와 일반 세이브 분리 (#110)

M2 완료 PR: #107, #108, #109, #110

## M3. 모바일 HUD / 맵 UI 정리 — 완료

모바일 화면의 정보 위계와 빈 공간을 맵과 전투 HUD로 나눠 정리했다.

- [x] 상단 HUD를 핵심 자원 / 부가 정보로 그룹화 (#111, #112)
- [x] 중복되거나 내부 구현에 가까운 표시 제거 (#112)
- [x] 맵 높이와 노드 간격을 모바일 화면 밀도에 맞게 축소 (#111)
- [x] 현재 선택 가능 / 잠김 / 완료 노드의 대비 강화 (#111)
- [x] 연결선 가독성 개선 (#111)
- [x] 카드 덱 미리보기의 잘림과 과도한 가로 스크롤 수정 (#111)
- [x] 버튼과 단순 정보 박스의 시각적 위계를 분리 (#111, #112)

M3 완료 PR: #111, #112

## M4. 신규 효과 카드 소량 실험 — 구현 4차까지 완료 / 체감 검증 진행 중

개발자 모드와 모바일 기본 UI가 테스트 가능한 수준이 된 뒤 시작했다.

- 한 번에 2~5장
- 전 지역 공용 풀
- 카드군 패시브 없음
- 각 카드마다 검증하려는 핵심 선택을 하나 이상 명시
- 기존 순수 카드의 단순 상위호환 방지
- 구현 → 자동 테스트 → 개발자 모드 체감 테스트 → 유지/수정/삭제 순환

우선 탐색 축:

- 현재 트릭 승리와 미래 쇼다운 사이의 교환
- 직전 트릭 승/패를 이용한 추격과 연승
- 쇼다운 슬롯 위치 활용
- 칩 교환을 실제 전투 선택으로 연결
- 리버 적중을 노리는 고위험 카드

### 실험 배치 진행 현황

- [x] 1차 실험 카드 3장 구현 및 자동 테스트 (#113)
- [x] 1차 DEV 체감 테스트 절차 문서화 (#119)
- [x] 2차 특이 카드 3장 구현 및 회귀 테스트
- [x] 3차 특이 카드 3장 구현 및 회귀 테스트
- [x] 4차 특이 카드 3장 구현 및 회귀 테스트
- [ ] 1~4차 **12장 전체를 같은 기준으로 DEV 체감 테스트**
- [ ] 카드별 `유지 / 수치 수정 / 효과 수정 / 삭제` 판정 기록
- [ ] 체감 결과가 정리되기 전 M4 5차 배치 추가 금지

1차 절차: [`docs/m4-first-batch-playtest.md`](docs/m4-first-batch-playtest.md)

## M4.5. RUN V3 + 빌드 아이덴티티 통합 안정화 — 최우선

최신 코드 점검에서 발견한 버그와 통합 리스크를 먼저 처리한다.

### P0 — 확정 버그

#### [x] P0-1. 필드 적용 시 트럼프 보너스 표시를 실제 계산값과 동기화 — 완료

점검 결과 실제 판정, HUD, 카드 상세 계산은 `TrumpFields` 계층에서 현재 `rulesOverride.trumpBonus`를 읽고 있었지만, 트럼프 용어 설명은 여전히 기본 `+3` 중심의 고정 설명이었다. 2026-08-26에 용어 설명도 현재 전투값을 직접 읽게 통일하고 표시 회귀 범위를 전 필드 값으로 확대했다.

완료 내용:

- [x] HUD의 트럼프 표시가 현재 `rulesOverride.trumpBonus`를 사용
- [x] 카드 상세 계산의 트럼프 단계가 실제 적용 보너스를 표시
- [x] 트럼프 용어 상세/요약이 현재 전투의 실제 보너스를 표시
- [x] `무법지대 0`, `감쇠 지대 +1`, `넓은 테이블 +2`, 기본 `+3`, `과열 테이블 +4`, `과충전 구역 +5`, `왕실 중계소 +6` 회귀 테스트 추가
- [x] 실제 판정값과 HUD·카드 상세·용어 설명 값이 같은지 브라우저 회귀 테스트로 검증
- [x] 일반 테스트 / 모바일 스모크 / 브라우저 스모크 통과

관련 커밋: `7b22d3c`, `0a5eb7a`

#### [x] P0-2. 후반 `분실물 보관소` 카드 영구 소실 방지 — 완료

`분실물 보관소`의 반환 조건이 `다음 일반 지역 진입`인데, 기존에는 두 번째 지역·최종 관문·최종지역에서도 공용 이벤트 후보가 될 수 있어 보관 카드가 영구 소실될 수 있었다. 2026-08-26에 이벤트 후보 조건과 저장 복구 경로를 함께 수정했다.

완료 내용:

- [x] 앞으로 방문할 일반 지역이 남아 있을 때만 `분실물 보관소`를 이벤트 후보에 포함
- [x] 두 번째 지역 이후 `storeCard` 직접 호출도 `no_future_region`으로 차단하여 덱에서 카드를 제거하지 않음
- [x] 구버전 저장에 이미 남아 있는 보관 카드는 최종 관문/최종지역 스테이지 진입 훅에서 +1 강화 후 자동 반환
- [x] 구세이브에서 후반 `분실물 보관소` 이벤트가 이미 열린 상태여도 카드를 빼지 않고 안전하게 이벤트 종료
- [x] 공통지역·첫 지역에서는 기존처럼 다음 일반 지역 진입 시 정상 반환
- [x] 후반 후보 차단 / 직접 보관 차단 / 구세이브 복구 / 저장 왕복 회귀 테스트 추가
- [x] 일반 자동 테스트 및 모바일 브라우저 스모크 통과

관련 커밋: `f28d994`, `b7fc131`

### P1 — 높은 우선순위 개선 / 회귀 방지

#### [x] P1-1. RUN V3 전체 브라우저 E2E 스모크 추가 — 완료

2026-08-26에 390×844 모바일 Chrome 환경에서 실제 UI 클릭으로 RUN V3 전체 진행을 관통하는 E2E를 Browser Smoke에 추가했다. 전투 카드 사용 자체는 기존 모바일 스모크가 실제 클릭으로 계속 검증하고, RUN V3 E2E는 전투 화면 진입 후 테스트용 승리 처리로 런 연결·보상·지역 전환·저장 복구·결과 화면을 집중 검증한다.

완료 내용:

- [x] 스타터/특성 실제 UI 선택 후 런 시작
- [x] 공통지역 전투·이벤트·캠프·엘리트 완료
- [x] 첫 지역 선택 → 우측 내부 분기 → 상점 → 엘리트 → 지역 보스 관통
- [x] 첫 지역 분기 직후 체크포인트 저장 → 페이지 새로고침 → `계속하기` 실제 클릭 → 런 지문/스테이지/지역 상태 동일성 검증
- [x] 두 번째 지역 선택 및 다른 지역인지 검증 → 내부 분기 → 지역 보스 관통
- [x] 최종 관문 이벤트/전투/엘리트 → 최종지역 진입
- [x] 최종지역 전투/엘리트/캠프/최종 보스 → `런 클리어` 결과 화면 검증
- [x] 방문 지역 2개 / 완료 지역 2개 / 분기 기록 2개 / 여정 기록 2개 검증
- [x] 브라우저 런타임 예외 0개 검증
- [x] 공용 CDP 클릭/대기 헬퍼 분리
- [x] 기존 모바일 브라우저 스모크와 일반 자동 테스트 동시 통과

관련 커밋: `19c4a60`, `69decbb`, `c3ff515`

#### [x] P1-2. 공통지역 `각인`이 기존 효과/네임드 카드를 조용히 덮어쓰지 않게 수정 — 완료

2026-08-26에 공통지역 카드 보상의 각인 대상을 같은 숫자·무늬의 **순수 카드만** 자동 선택하도록 고정했다. 같은 슬롯에 효과 카드나 네임드만 남아 있으면 각인 버튼 자체를 비활성화하고, 액션을 직접 호출해도 덱이나 노드 진행을 바꾸지 않는다.

완료 내용:

- [x] 각인 대상 탐색을 UI와 액션이 같은 순수 카드 판정 헬퍼로 공유
- [x] 순수 대상이 없으면 `각인 불가` 버튼을 표시하고 비활성화
- [x] 순수 대상이 없는 `engrave` 직접 호출은 `no_pure_target`을 반환하며 덱·오버레이·노드 진행을 변경하지 않음
- [x] 같은 슬롯에 효과 카드/네임드만 있는 경우 기존 카드가 그대로 보존되는 회귀 테스트 추가
- [x] 같은 슬롯에 효과 카드와 순수 카드가 함께 있으면 순수 카드만 각인되는 회귀 테스트 추가
- [x] `추가` 보상은 기존처럼 새 카드를 덱에 더하는 동작 유지
- [x] 일반 자동 테스트 / 모바일 브라우저 스모크 / RUN V3 전체 브라우저 E2E / Pages 배포 통과
- [x] Chrome CDP 캠프 클릭 변동성을 줄이기 위해 버튼 내부 안전 지점 클릭과 짧은 후처리 대기 적용

관련 커밋: `1d80d9e`, `66570fe`, `e846a88`, `f4f295b`

#### [ ] P1-3. 스타터 4종 × 특성 8종 × 필드 8종의 최소 체감 검증

전 조합 256개를 전수 플레이할 필요는 없지만, 최소한 각 요소가 한 번 이상 포함되도록 대표 조합을 구성한다.

검증 항목:

- 특정 스타터가 초반 공통지역을 지나치게 쉽게/어렵게 만들지 않는가
- 특정 특성이 사실상 무조건 선택되거나 무의미하지 않은가
- 손패 -1 필드에서 플레이가 막히지 않는가
- 낮은 수 승리 필드가 적 AI/예측 UI와 모순되지 않는가
- 트럼프 0~6 변형이 카드 가치와 적 난이도를 과도하게 흔들지 않는가
- 빌드 태그가 설명용 라벨에 그치지 않고 실제 보상 선택을 돕는가

#### [ ] P1-4. 이벤트 피해의 `최저 HP 1` 정책 확정

현재 이벤트 피해는 플레이어 HP를 최소 1에서 멈춘다. 의도된 비치명 규칙이라면 UI에 명시하고, 의도가 아니라면 일반 패배 처리와 연결한다.

완료 조건:

- `이벤트 피해는 비치명` 또는 `이벤트로도 사망 가능` 중 하나를 규칙으로 확정
- 위험 선택 UI가 실제 위험도를 정확히 표시
- HP 1~3 경계값 회귀 테스트 추가

### P2 — 품질 개선

#### [ ] P2-1. `마술 상자` 힌트 카드 선택을 덱 첫 장 고정에서 시드 기반 샘플로 변경 검토

현재 힌트 카드가 사실상 덱 배열의 첫 장으로 정해진다. 이벤트의 의미가 `덱 맨 앞 확인`이 아니라면 RUN V3 시드에 묶인 결정적 랜덤 샘플이 자연스럽다.

- 같은 시드/같은 진행 상태에서는 같은 카드
- 다른 시드에서는 변화 가능
- 저장/불러오기 전후 결과 동일

#### [ ] P2-2. 트럼프 `+3` 전용 회귀 테스트 이름/가정 정리

기본 규칙의 `+3` 검증은 유지하되, 필드가 트럼프 보너스를 바꿀 수 있는 현재 구조에서는 `기본값 +3`과 `현재 전투 보너스`를 구분한다.

#### [ ] P2-3. 이정표와 main 상태 동기화 규칙 추가

대규모 기능 통합 커밋 시 다음 중 하나를 같이 갱신한다.

- `현재 main 스냅샷`
- 해당 마일스톤 진행 상태
- 즉시 다음 작업

## M5. 밸런스 / 플레이테스트

M4.5의 P0/P1이 끝난 뒤 본격 진행한다.

- 초반 일반전 HP 12 / 강전투 18 / 엘리트 32 / 보스 60 기준 체감 확인
- 쇼다운 차이 피해가 실제 전투 길이에 맞는지 확인
- 순수 카드와 효과 카드의 선택 가치 비교
- 칩 최대 5와 교환 비용 2의 순환성 확인
- 폴드 8 HP 비용과 장기 세트 운영의 실제 가치 확인
- 압승 / 대압승은 연출 전용으로 유지하면서 발생 빈도 확인
- 8스테이지 전체 런 평균 길이와 전투/이벤트/상점 비율 측정
- 첫 지역 선택과 두 번째 지역 선택이 실제로 다른 플레이 경험을 만드는지 확인
- 스타터/특성/필드/네임드가 하나의 빌드 방향으로 읽히는지 확인

---

## 즉시 다음 작업

1. M4 1~4차 12장 DEV 체감 테스트
2. **P1-3 스타터·특성·필드 대표 조합 플레이테스트**
3. **P1-4 이벤트 피해의 최저 HP 1 정책 확정**
4. P2 품질 개선 항목 순차 처리

새 카드나 새 시스템을 더 추가하는 작업은 위 안정화 항목이 끝난 뒤 재개한다.